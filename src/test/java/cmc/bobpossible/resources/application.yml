spring:
  datasource:
    url: jdbc:mariadb://missionbobpossible.cveg9tdkeygv.ap-northeast-2.rds.amazonaws.com/dev?autoReconnect=true
    username: admin
    password: bobpossible1
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      #      show_sql: true
      format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace